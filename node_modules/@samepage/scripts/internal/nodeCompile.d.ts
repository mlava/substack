import esbuild, { BuildOptions } from "esbuild";
type Args = {
    outdir?: string;
    functions: string[];
    root?: string;
    define?: Record<string, string>;
    opts?: esbuild.BuildOptions;
    external?: string[];
};
export declare const getOpts: ({ outdir, functions, root, define, opts, external, }: Args) => BuildOptions;
declare const nodeCompile: (args: Args) => Promise<esbuild.BuildResult<esbuild.BuildOptions>>;
export default nodeCompile;
