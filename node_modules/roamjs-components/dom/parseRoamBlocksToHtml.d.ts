import { getParseInline, RoamContext } from "../marked";
import type { TreeNode, ViewType } from "../types";
export declare const syncParseRoamBlocksToHtml: ({ content, viewType, level, context, parseInline, }: {
    level: number;
    context: Required<RoamContext>;
    content: TreeNode[];
    viewType: ViewType;
    parseInline: Awaited<ReturnType<typeof getParseInline>>;
}) => string;
declare const parseRoamBlocksToHtml: (args: Omit<Parameters<typeof syncParseRoamBlocksToHtml>[0], "parseInline">) => Promise<string>;
export default parseRoamBlocksToHtml;
