"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const createHTMLObserver_1 = tslib_1.__importDefault(require("./createHTMLObserver"));
const createButtonObserver = ({ attribute, render, shortcut = attribute, }) => (0, createHTMLObserver_1.default)({
    callback: (b) => {
        if (b.innerText.toUpperCase() ===
            attribute.toUpperCase().replace(/-/g, " ") ||
            b.innerText.toUpperCase() === shortcut.toUpperCase()) {
            const dataAttribute = `data-roamjs-${attribute}`;
            if (!b.getAttribute(dataAttribute)) {
                b.setAttribute(dataAttribute, "true");
                render(b);
            }
        }
    },
    tag: "BUTTON",
    className: "bp3-button",
    useBody: true,
});
exports.default = createButtonObserver;
//# sourceMappingURL=createButtonObserver.js.map