"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const elToTitle = (e) => {
    if (!e) {
        return "";
    }
    else if (e.nodeName === "#text") {
        return e.nodeValue || "";
    }
    else if (e.nodeName === "SPAN" &&
        e.classList.contains("rm-page-ref__brackets")) {
        return "";
    }
    else if (e.nodeName === "SPAN" &&
        e.classList.contains("rm-page-ref")) {
        return `[[${Array.from(e.childNodes).map(elToTitle).join("")}]]`;
    }
    else {
        return Array.from(e.childNodes).map(elToTitle).join("");
    }
};
exports.default = elToTitle;
//# sourceMappingURL=elToTitle.js.map