import type { RoamBasicNode } from "../types/native";
declare const addKeybordTriggers: ({ triggers: inputTriggers, }: {
    triggers: {
        trigger: RegExp | (() => RegExp) | {
            tree: RoamBasicNode[];
            defaultValue?: string;
        };
        callback: () => void;
    }[];
}) => void;
export default addKeybordTriggers;
