"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getMutatedNodes = ({ ms, tag, className, nodeList, }) => {
    const blocks = ms.flatMap((m) => Array.from(m[nodeList]).filter((d) => d.nodeName === tag &&
        Array.from(d.classList).includes(className)));
    const childBlocks = ms.flatMap((m) => Array.from(m[nodeList])
        .filter((n) => n.hasChildNodes())
        .flatMap((d) => Array.from(d.getElementsByClassName(className))));
    return [...blocks, ...childBlocks];
};
exports.default = getMutatedNodes;
//# sourceMappingURL=getMutatedNodes.js.map