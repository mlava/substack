declare const getMutatedNodes: ({ ms, tag, className, nodeList, }: {
    ms: MutationRecord[];
    tag: string;
    className: string;
    nodeList: "addedNodes" | "removedNodes";
}) => Node[];
export default getMutatedNodes;
